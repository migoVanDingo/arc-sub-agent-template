"""Example arc sub-agent.

The package layout:
  arc_sub_agent_example/
    spec.py              — the SubAgentSpec + the entry-point `build` function
    prompts/system.md    — the system prompt as an external markdown file

When you fork this template:
  1. Rename the package directory (`src/arc_sub_agent_example/` →
     `src/arc_sub_agent_<yours>/`)
  2. Update `pyproject.toml` (name, entry-point key, packages target,
     force-include path for prompts/)
  3. Update imports in `spec.py` and `tests/`
"""
__version__ = "0.1.0"
